{
  "Servers": {
    "1": {
      "Name": "LMS Local",
      "Group": "Servers",
      "Host": "db",
      "Port": 5432,
      "MaintenanceDB": "lmsdb",
      "Username": "lmsuser",
      "SSLMode": "prefer",
      "Comment": "Host is 'db', not localhost: pgAdmin connects over the compose network, where the Postgres container is reachable by its service name."
    }
  }
}
